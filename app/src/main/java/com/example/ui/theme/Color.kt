package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Dark Mode Color Palette
val SlateBg = Color(0xFF090D14)          // OLED deep dark blue-slate background
val GlassSurface = Color(0xFF131D2F)     // Glassmorphic card surface
val GlassBorder = Color(0x1AFFFFFF)      // 10% translucent white for glass border shine
val GlassBorderHighlight = Color(0x33FFFFFF) // 20% translucent white for highlights
val MintGreen = Color(0xFF10B981)        // Emerald health/food accent (Primary)
val MintNeon = Color(0xFF34D399)         // Glowing mint green for active states
val IceBlue = Color(0xFF38BDF8)          // Ice blue for AI / Chat elements
val SunsetOrange = Color(0xFFF59E0B)     // Golden amber for secondary highlights
val CharcoalDark = Color(0xFF111827)     // Matte charcoal contrast background
val SoftSilver = Color(0xFFE2E8F0)       // Clean silver for body headings
val MutedText = Color(0xFF94A3B8)        // Slate gray for secondary details
val ShadowColor = Color(0x56000000)      // Deep ambient shadows

